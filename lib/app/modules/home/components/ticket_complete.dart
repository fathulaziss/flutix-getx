import 'package:flutter/material.dart';

class TicketComplete extends StatelessWidget {
  const TicketComplete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Ticket Complete'));
  }
}
