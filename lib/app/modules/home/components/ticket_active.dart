import 'package:flutter/material.dart';

class TicketActive extends StatelessWidget {
  const TicketActive({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Ticket Active'));
  }
}
