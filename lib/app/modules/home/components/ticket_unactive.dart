import 'package:flutter/material.dart';

class TicketUnactive extends StatelessWidget {
  const TicketUnactive({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Ticket Unactive'));
  }
}
